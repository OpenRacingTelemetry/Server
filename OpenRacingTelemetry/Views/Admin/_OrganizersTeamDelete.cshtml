@model string
@using OpenRacingTelemetry.Models

<form asp-action="OrganizersTeamDelete" role="form">
    @Html.Partial("_ModalHeader", new ModalHeader { Heading = "Delete Organizers Team" })

    <div class="modal-body form-horizontal">
        Are you want to delete @Model?
    </div>
    @Html.Partial("_ModalFooter", new ModalFooter { SubmitButtonText = "Delete" })
</form>

